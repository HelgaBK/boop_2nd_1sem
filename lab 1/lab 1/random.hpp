#pragma once

//generates random double from 0 (included) to 1 (not included)
double getRandomDouble();

//generates random integer from 0 (included) to upperBound (not included)
unsigned int getRandomInt(unsigned int upperBound);
